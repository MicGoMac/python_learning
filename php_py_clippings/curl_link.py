function getCURL ( $theLink ) {    // create a new cURL resource    $ch = curl_init();    curl_setopt($ch, CURLOPT_URL, $theLink );    curl_setopt($ch, CURLOPT_HEADER, 0);    curl_setopt($ch, CURLOPT_RETURNTRANSFER,1);  // grab URL and return string instead of passing it to the browser    $s= curl_exec($ch);     // close cURL resource, and free up system resources    curl_close($ch);    return $s;}