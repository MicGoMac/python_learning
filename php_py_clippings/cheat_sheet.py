hay first	str_replace( $search, $replace, $subject )	strpos ( hay, needle )	substr( str , start, length )	array_diff( $sourcearray,  $compare_array...)needle first	in_array ( needle hay )	explode ( delimiter, array )	implode ( glue, array )	array_search( needle, hay)	array_key_exists( key, array )	array_key_exists ( mixed $key , array $array )file_put_contents( filename, data)