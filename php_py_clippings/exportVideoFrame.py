function exportVideoFrame($fullfile){	$bname=basename($fullfile, ".dv");	echo $bname."\n\n\n";	//may also want it's parent dir name	$pname=dirname($fullfile);	$pdir=basename($pname);		$opname=$pdir."-".$bname;	$opname=str_replace( " ", "_", $opname);	$opname=str_replace( ";", "_", $opname);	$opname=str_replace( "clip-", "", $opname);		//added ss to later frame	$cmd = "ffmpeg -ss 150 -i '$fullfile' -r 1 -vframes 1 $opname"."%1d.jpg";	print $cmd;	exec($cmd);	}